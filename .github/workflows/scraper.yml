name: Scraper Loto Honduras

on:
  # Ejecutar automáticamente 3 veces al día (5 minutos después de cada sorteo)
  schedule:
    - cron: '05 17 * * *'  # 11:05 AM Honduras = 5:05 PM UTC
    - cron: '05 21 * * *'  # 3:05 PM Honduras = 9:05 PM UTC
    - cron: '05 3 * * *'   # 9:05 PM Honduras = 3:05 AM UTC (día siguiente)
  
  # Permitir ejecución manual desde GitHub
  workflow_dispatch:

jobs:
  scrape:
    runs-on: ubuntu-latest
    
    steps:
      # 1. Clonar el repositorio
      - name: 📥 Checkout repository
        uses: actions/checkout@v3
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      
      # 2. Configurar Python
      - name: 🐍 Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
          cache: 'pip'
      
      # 3. Instalar dependencias
      - name: 📦 Install dependencies
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt
      
      # 4. Ejecutar el scraper
      - name: 🔄 Run scraper
        run: |
          python loto_scraper.py
          echo "✅ Scraper ejecutado exitosamente"
      
      # 5. Verificar si hay cambios o si es archivo nuevo
      - name: 📊 Check for changes
        id: verify_diff
        run: |
          git add resultados_hoy.json
          if git diff --staged --quiet; then
            echo "changed=false" >> $GITHUB_OUTPUT
          else
            echo "changed=true" >> $GITHUB_OUTPUT
          fi
      
      # 6. Commit y push de los cambios
      - name: 💾 Commit and push changes
        if: steps.verify_diff.outputs.changed == 'true'
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "GitHub Actions Bot"
          git commit -m "🎯 Actualizar resultados - $(date +'%Y-%m-%d %H:%M:%S')"
          git push
      
      # 7. Mensaje si no hay cambios
      - name: ℹ️ No changes detected
        if: steps.verify_diff.outputs.changed != 'true'
        run: echo "ℹ️ No se detectaron cambios en los resultados"